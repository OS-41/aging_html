# aging APIのAPIキー(必須)。このファイルをコピーして .env を作成し、値を設定する。
AGING_API_KEY=

# このサーバーの公開URL(任意)。設定するとaging APIに渡す画像URLの組み立てに
# クライアント申告のoriginではなくこの値を使う。
# 例: PUBLIC_ORIGIN=https://xxxx-5000.app.github.dev
PUBLIC_ORIGIN=
